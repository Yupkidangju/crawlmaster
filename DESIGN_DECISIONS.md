# DESIGN_DECISIONS.md (설계 의사결정 이력)

본 문서는 **Crawlmaster** 프로젝트 개발 과정에서 내려진 주요 설계 의사결정(Architecture & System Design Decisions)과 기각된 대안, 그리고 그에 따른 기술적 근거를 기록한다.

---

## 1. 그래픽스 및 입력 라이브러리 선정
* **결정:** SFML 2.6.x (Simple and Fast Multimedia Library) 채택
* **배경:**
  데스크톱 앱으로 C++에서 1인칭 와이어프레임 3D 렌더링을 소프트웨어 방식으로 그릴 때, 윈도우 컨텍스트 생성과 키보드 입력, 선 및 도형 드로잉 인터페이스가 가벼워야 했다.
* **대안 및 기각 사유:**
  * **Raylib:** 매우 훌륭하고 기본 3D 함수들을 지원하지만, 직접 원근 와이어프레임 투영을 구현하여 옛 복고풍 80년대 PC 감성(선 기반 사영)을 커스텀하게 연출하기에는 SFML의 2D Vector Drawing(`sf::VertexArray`)을 조작하는 것이 제어권 측면에서 더 유리하다고 판단했다.
  * **DirectX / OpenGL 직접 제어:** 렌더링 엔진 스캐폴딩 코드가 너무 길어져 Phase 1의 기획 및 핵심 루프 완수 속도를 저해하고, 유지보수가 과도하게 복잡해진다.
  * **터미널 ASCII (ncurses):** CLI 환경으로 가두기보다 SFML 기반 윈도우 데스크톱 창으로 배포함으로써 다채로운 색감 표현 및 정교한 복고풍 DOS 비트맵 폰트 매핑을 보장하기 위해 기각함.
* **의사결정 결과:**
  SFML 2.6.x를 CMake FetchContent 방식으로 프로젝트 로컬에 끌고와 고정 해상도 1024x768 윈도우 창에서 `sf::RenderWindow`와 `sf::VertexArray`를 통해 드로잉 제어를 동결함.

---

## 2. D&D 5e 규칙 단순화 범위 결정
* **결정:** 6대 능력치, 능력치 보정치 공식, AC 계산식, d20 주사위 롤링 코어를 동결하되, 복잡한 5e 규칙(주문 피트, 다중 행동, 피트 선택)은 전면 기각함.
* **배경:**
  D&D 5e는 고도로 다듬어진 TRPG 규칙이지만 컴퓨터 게임으로 그대로 완전 이식할 경우 시스템 경계 계약(`spec.md`)이 방대해진다. Phase 1의 성공 기준은 턴제 전투의 기본 동작과 확장 가능한 추상 모델을 검증하는 것이다.
* **대안 및 기각 사유:**
  * **D&D 5e 원본 그대로 구현 (주문 레벨, 소마법, 반응행동, 기회공격 등 포함):** 전투 엔진 클래스의 복잡도가 기하급수적으로 올라가며, AI 에이전트와 구현자 간의 정합성 불일치를 유발할 확률이 높으므로 기각.
  * **기본 RPG 규칙 (능력치 없음, 단순 공격/방어 수치만 사용):** D&D의 감성을 원한 사용자의 요구사항(5번: "D&D스타일의 캐릭터 구조와 스킬")에 위배되므로 기각.
* **의사결정 결과:**
  * 명중은 `1d20 + 보정치 + 숙련보너스` vs `타겟 AC`로 고정.
  * 피해는 `무기 주사위(1d4 ~ 1d8) + 능력치 보정치`로 고정.
  * 마법 및 성직자 주문은 복잡한 슬롯 회복 대신 1레벨 슬롯 2개(마을 휴식 시 충전)로 제한하며, 스킬은 MP 대신 고정 쿨타임 또는 사용 횟수로 간소화.

---

## 3. 던전 생성 알고리즘
* **결정:** DFS(깊이 우선 탐색, Recursive Backtracking) 기반 미로 생성 후 임의 벽 해체 패턴 채택.
* **배경:**
  정통 Wizardry는 미로 전체가 닫힌 벽과 길로 구성된 던전 그리드 구조다. BSP(Roguewarp 구조)는 넓은 방 중심이므로 Wizardry 특유의 좁은 복도 코너 탐색 뷰에 부적합하다.
* **대안 및 기각 사유:**
  * **BSP (Binary Space Partitioning):** 방과 방 사이를 연결하는 알고리즘으로, 복도 위주의 1인칭 미로 탐험 스타일의 미스터리함을 구현하기에는 적합도가 떨어진다.
  * **Prim 알고리즘:** DFS와 유사하게 미로를 만들지만 복도가 지나치게 짧고 좁아지는 경향이 있어, 긴 직진 복도와 코너가 살아있는 DFS가 1인칭 원근 뷰 연출에 가장 심미적으로 훌륭했다.
* **의사결정 결과:**
  DFS로 완벽한 미로를 만든 후, 생성기가 끝난 뒤 미로 상의 임의의 벽 5~10개를 무작위로 제거해 순환 고리(Loop)를 만들어 플레이어가 막다른 길에만 갇히지 않도록 수정 설계함.

---

## 4. 세이브 파일 및 리셋 정책
* **결정:** UTF-8 JSON 기반 versioned per-user save와 원자 교체/백업을 사용하며, 파티 전멸은 현재 런만 종료하고 마지막 town checkpoint를 복구한다.
* **배경:**
  고전 RPG의 긴장감은 현재 런의 실패로 표현한다. 저장 손상 복구와 의도적인 New Game/TPK를 같은 초기화 명령으로 합치면 정상 진행이 비가역적으로 사라지므로 서로 다른 typed command로 분리한다.
* **대안 및 기각 사유:**
  * **TPK 즉시 파일 초기화:** 입력 확인과 복구 수단이 없고 손상 복구와 구분되지 않아 기각했다.
  * **활성 던전 전체 저장:** 한 층 데모의 구현·마이그레이션 복잡도를 키우므로 현재 lane에서는 기각하고 town checkpoint만 저장한다.
* **의사결정 결과:**
  * `save.json`과 `config.json`은 OS별 per-user directory에 기록하며 schema v2를 사용한다.
  * 같은 디렉터리 임시 파일을 flush/fsync한 뒤 `.bak` 회전과 원자 교체를 수행한다.
  * 손상 파일은 quarantine하고 유효 백업을 우선 복구한다. 자동 초기화는 금지한다.
  * TPK는 `GameOverState`를 거쳐 마지막 town checkpoint를 다시 로드한다. New Game만 명시 확인 후 초기 저장을 만든다.

---

## 5. 미니맵 오토맵 시각화 및 자동 이동 조작
* **결정:** 미니맵 내 안개가 걷힌 지역 마우스 왼쪽 클릭을 통한 BFS(너비 우선 탐색) 기반 최단 경로 자동 이동 지원 및 색상 고도화 ([v0.6.0]).
* **배경:**
  기존에는 복고풍 키보드 단독 조작을 지향하였으나, 탐색이 끝난 복도를 다시 돌아가거나 퀘스트 완료 후 계단으로 돌아갈 때의 지루한 키 반복 입력을 개선하기 위해 마우스 조작을 보조 수단으로 허용하여 편의성을 보강했다.
* **대안 및 기각 사유:**
  * **A* (A-Star) 알고리즘:** 모든 셀의 가중치(이동 비용)가 1로 동일한 20x20 크기의 소형 그리드 맵이므로, 휴리스틱 계산 오버헤드가 있는 A* 대신 가장 직관적이고 연산이 가벼운 BFS 알고리즘만으로도 완벽한 최단 경로 도출이 보장되므로 기각함.
  * **미탐색 지역 자동 이동:** 던전 탐색의 안개 걷기(Fog of War) 퍼즐적 가치를 지키기 위해 아직 방문하지 않아 밝혀지지 않은 타일(`visited == false`)로의 자동 이동은 엄격히 차단한다.
* **의사결정 결과:**
  * 미니맵 영역 `(750x40 ~ 990x280)` 마우스 클릭 감지 시 격자 인덱스를 역산하고, 목적지가 `visited` 및 `walkable`인지 검증한다.
  * 한 단계 이동마다 플레이어 시선 방향을 동적 회전하고 주변 시야 안개를 지속 갱신(stepped & visited)한다.
  * 자동 이동 중에도 매 격자 이동 시마다 10%의 인카운터 주사위를 굴리며, 몬스터 조우 시 자동 이동을 중단하여 긴장감과 규칙 정합성을 완벽히 만족시킨다.

---

## 6. 캐릭터 관리 및 인벤토리 제어 설계
* **결정:** 마을 및 던전 상태 스택 FSM(`pushState`/`popState`) 기반 통합 `CharacterInfoState` 구성 및 양방향 직관적 장비/인벤토리 스왑 패턴 적용 ([v0.6.0]).
* **배경:**
  아이템 획득 및 장비 교체는 던전 탐험 RPG의 핵심 보상 주기다. 이전 상태를 완전히 파괴하는 `changeState`를 쓰면 인벤토리를 닫을 때 이전 모험 진행 및 마을 UI 좌표가 리셋되므로, 이전 상태 메모리를 완벽 보존하는 `pushState`를 채택해 화면 전이를 원활하게 처리했다.
* **대안 및 기각 사유:**
  * **던전/마을 내 미니 인라인 윈도우 팝업:** 메인 탐험 클래스의 드로잉 루프가 지저분해지고 UI 관심사의 분리(Separation of Concerns)를 위배하므로 기각함.
  * **개별 캐릭터 가방 모델:** 파티 공용 가방 인벤토리를 차용하여 복잡한 캐릭터 간 아이템 넘겨주기 조작 단계를 제거하고, 장비 착용 시에만 스왑이 동작하게 단순화하여 UX 편의성을 보장함.
* **의사결정 결과:**
  * `I`/`C` 키로 인벤토리 상태를 스택에 푸시하고, 좌우 분할 포커스(Tab/방향키) 방식을 채택하여 조작을 일원화하였다.
  * 인벤토리 장비 선택 시 자동 스왑 장착 및 이전 장비 가방 회수 로직을 적용하고, 마법사/도적 클래스는 무거운 중장비 착용을 차단하여 D&D 룰의 밸런스와 오리지널리티를 보증하였다.

---

## 7. 객체지향형 스킬(Skill) 클래스 아키텍처 및 상태이상/버프 설계
* **결정:** 전투와 직업에 얽혀 하드코딩되어 있던 스킬/주문 연산을 다형성 기반 `Skill` 추상 인터페이스 및 구체 파생 클래스로 객체지향화하고, 턴 기반 상태이상(독, 마비) 및 버프(STR/DEX 증가, 축복) 관리를 위한 구조화된 버퍼 시스템 탑재.
* **배경:**
  기존의 클래스별 전투 스킬은 `CombatState::performPlayerSkill` 내에서 하드코딩 분기로 작성되어 스펙 확장 및 신규 주문 추가 시 코드가 비대해지고 정합성 검증이 어려웠다. 볼륨 확장에 유연하게 대처하기 위해 스킬별 독자 실행 로직을 클래스 단위로 분리하고, 물약 다변화에 맞춰 캐릭터 시트에 턴 기반 상태/버프 관리 변수를 통합해야 했다.
* **대안 및 기각 사유:**
  * **전투 상태 내 분기 유지 및 확장:** 전투 클래스인 `CombatState`가 지나치게 무거워져 결합도(Coupling)가 극대화되고 유지보수 및 유닛 테스트가 곤란해지므로 기각함.
  * **전용 MP/마나 포인트 도입:** D&D 원작 5e의 주문 슬롯(Spell Slots) 시스템이 이미 구현되어 있고, 마법사와 성직자의 정체성을 살리기 위해 별도 마나 게이지를 신설하지 않고 주문 슬롯을 회복하는 마나 물약을 기획하여 D&D 오리지널리티를 지켰다.
* **의사결정 결과:**
  * `Skill` 추상 클래스를 작성하여 `execute` 가상 함수에 시전자, 아군 파티 목록, 적 몬스터 목록, 타겟 인덱스, 로그 출력 스트림을 전달함으로써 모든 종류의 광역, 단일, 자가 치유, 버프/디버프 주문 및 스킬을 유연하게 소화하도록 설계함.
  * `Character` 및 `Monster` 모델에 독(`poisonTurns`), 마비(`paralysisTurns`) 변수 및 `processTurnEffects` 인터페이스를 제공하여 턴 시작 시 피해 및 행동 제한을 처리함.
  * 전투 중 스킬 시전 시 전용 선택 팝업창을 띄워 유동적으로 사용할 스킬 번호를 조작하도록 TUI 모드를 확장 구현함.

---

## 8. 다국어(i18n) 설정 매니저 설계 및 설정 진입 단축키의 O키 변경 결정
* **결정:** 다국어 리소스를 JSON 기반 싱글톤(`LocalizationManager`)으로 관리하고, 게임 플레이 도중 설정 진입 단축키를 W/A/S/D 이동 방향키(특히 S키 후진)와의 중복 충돌을 방지하기 위해 `O` 키(Options)로 최종 변경 및 통일함.
* **배경:**
  초기 기획 단계에서는 설정(Settings) 상태 진입 단축키로 `S` 키(Settings의 S)를 차용했으나, 던전 탐험 상태(`DungeonState`)에서 `W/A/S/D`를 방향키로 조작하여 뒤로 이동하는 키와 중복되어 컴파일 시점에 `duplicate case value` 컴파일 에러를 격발시켰다. 이로 인해 이동 키와 충돌이 없으면서 옵션 메뉴 진입에 가장 범용적으로 인지될 수 있는 단축키가 필요했다.
* **대안 및 기각 사유:**
  * **S키 그대로 유지:** 던전 이동 중 뒤로 이동하는 조작을 완전히 유실하거나 키 배치를 변경해야 하므로 기각함.
  * **ESC 키를 설정 메뉴용으로 변경:** 이미 ESC 키는 하위 팝업(스킬 선택, 인벤토리 등) 닫기 및 마을/던전 퇴거 등에 범용 매핑되어 있어, 이를 설정 화면용으로 전환하면 상태 복귀 루프가 꼬이게 되므로 기각함.
* **의사결정 결과:**
  * 모든 씬(마을, 던전, 전투)에서 설정 화면에 진입하는 키를 `O` 키로 통일하여 키 조작 혼선을 방지하고 컴파일러 충돌 문제를 깔끔히 해결함.
  * 문자열 갱신은 렌더링 경로에서 수행한다. 다만 `sf::Text`는 폰트 객체를 복사하지 않고 참조하므로, 언어 전환 후 기존 지속 텍스트의 폰트 재바인딩과 CJK 혼합 문자열 화면 검증은 `docs/audit/audit_report_8.md`에 따른 미해결 개선 과제로 관리한다.
# v0.9.4 다국어 폰트 수명주기 결정

- 언어 전환으로 폰트 선택이 바뀌면 각 State가 지속 `sf::Text`에 현재 폰트를 다시 바인딩한다.
- CJK 폰트는 ASCII와 CJK가 함께 포함된 UI를 우선 고려해 시스템 Noto Sans CJK를 먼저 탐색한다.
- 외부 폰트의 재배포 허가와 provenance는 코드 판단으로 확정하지 않고 Human Review Required로 유지한다.

---

## 9. Turn 1 감사 후 제품 lane과 종결 범위

* **결정:** 현재 제품은 1.0 또는 Early Access가 아니라 Linux x86_64 기준 한 층짜리 상용 데모 후보로 관리한다.
* **배경:** 단일 재생성 미로에는 목표와 엔딩이 없어 기술 Phase 완료가 제품 완료로 잘못 표현됐다.
* **대안 및 기각 사유:** 다층 정식 게임을 즉시 구현하는 안은 저장·콘텐츠·UI·패키지 경계를 동시에 넓혀 검증 단위를 무너뜨리므로 기각했다.
* **결과:** 한 런은 30~60분 설계 목표로 시작 계단, 랜드마크 문, 보스 관문, Dragon Whelp, 결과 화면을 포함한다. 플레이 시간은 실제 측정 전까지 `UNVERIFIED`다. Windows Server 2022 hosted gate는 통과했지만 clean Windows 10/11/high-DPI/장시간 실기 전에는 상용 지원을 주장하지 않고 macOS는 범위 밖이다.

## 10. 세션 RNG와 전투 규칙 진실원

* **결정:** 모든 게임 난수는 한 세션이 소유하는 seedable `std::mt19937` stream에서 소비한다. 전투 명중/피해/보상과 장비 허용은 UI가 아니라 domain 규칙 계층이 판정한다.
* **배경:** 호출마다 `random_device`를 만들고 일부 경로가 C `rand()`를 사용하여 버그 재현과 밸런스 측정이 불가능했다.
* **대안 및 기각 사유:** 테스트 전용 mock RNG를 각 클래스에 따로 두는 방식은 production과 test path를 분리하므로 기각했다.
* **결과:** 세션 seed를 로그/저장 메타데이터에 남기고 테스트가 같은 seed를 주입한다. 자연 1/20, Bless, weapon dice count, resistance, STR/class requirement와 reward ledger를 고정 seed 테스트로 잠근다.

## 11. 명시적 선택과 파괴적 행동 정책

* **결정:** 모집은 preview/reroll/confirm, 전투 아이템과 단일 아군 스킬은 item/target/confirm/cancel, New Game과 판매/해고는 확인 단계를 사용한다.
* **배경:** 숨은 자동 선택과 단일 키 즉시 삭제는 플레이어 전략과 진행 안전을 동시에 훼손했다.
* **결과:** 효과가 없는 아이템/주문은 자원이나 턴을 소비하지 않는다. 취소는 상태와 저장 bytes를 바꾸지 않는다.

## 12. 재현 가능한 의존성 및 패키지

* **결정:** SFML 2.6.1과 nlohmann/json 3.11.3을 immutable commit으로 고정하고 정적 링크 기반 CMake install/CPack 패키지를 만든다.
* **배경:** 시스템 SFML 우선 탐색과 개발 build tree RUNPATH는 동일 소스가 다른 산출물을 만들고 재배치를 막았다.
* **결과:** Linux TGZ와 Windows ZIP을 대상으로 CTest와 package smoke를 CI에 두며 run `33786241695` attempt 2에서 양쪽 artifact와 native SLSA/SPDX attestation을 검증했다. 폰트 provenance와 별개인 법률/지원 주체는 사람 승인 전까지 차단 gate다.

## 13. 저장 가능한 RNG 위치와 공통 전투/i18n 경계

* **결정:** `SessionRng`는 seed와 원시 엔진 draw count를 저장 가능한 session checkpoint로 노출한다. 일반 공격과 공격형 Skill은 같은 `CombatRules`를 사용하고, 사용자 표시 문자열은 locale key+placeholder formatter를 통과한다.
* **배경:** seed만 기록하고 Continue에서 소비하지 않으면 실제 재현이 불가능하며, Skill별 자체 피해 계산과 model literal은 전투 및 언어 계약을 다시 분산시킨다.
* **대안 및 기각 사유:** `std::mt19937` stream 문자열 전체 저장은 구현체/표현 의존성과 큰 fixture를 만들므로 기각했다. 고수준 roll 호출 수만 저장하는 방식은 rejection sampling이 소비한 원시 draw 수를 복원하지 못하므로 기각했다.
* **결과:** 고정 seed와 raw draw count로 다음 난수를 이어가며, 무기 기반 Skill은 장착 무기 dice/type을 유지한다. UI는 ID를 저장하고 표시 시 현재 locale로 해석한다. 실제 raster와 법률 승인은 자동 key 검사와 분리한다.
